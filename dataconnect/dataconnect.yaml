specVersion: "v1beta"
serviceId: "musicacademyappact"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "musicacademyappact-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
